# 2026-09-06 第二轮独立盲测

## 测试目的

验证提交 `3276fa3` 对首轮独立盲测四项偏差的修复是否生效，并确认安装后的 `assess-enterprise-ai-project` Skill 能否在三个全新上下文中稳定区分 `GO`、`VALIDATE-FIRST` 和 `NO-GO`。

## 被测修复

1. 材料仅转述制度、报告、日志、审批或实验时，保持为 `documented_claim`；只有实际检查原始对象时才能使用 `verified_fact`。
2. `NO-GO` 的执行结论必须把决定性负面证据写为 `SUFFICIENT`，非决定性材料缺口另列。
3. 第一门只判断问题真实性和重要性，不能因后续方案、技术、ROI 或治理失败而降级。
4. `VALIDATE-FIRST` 必须收敛到一个具体品类或分群、一个有边界周期、一个 AI 节点、有限样本及明确排除项；材料没有给出具体值时列为启动前冻结项，不得编造。

## 隔离方法

- 启动三个全新的 Codex CLI 会话，每个会话使用独立的临时工作目录和只读沙箱。
- 每个工作目录只有一份重命名为 `input.md` 的项目材料。
- 提示只要求调用已安装 Skill 评审 `input.md`，并明确禁止读取案例库、历史输出、测试断言和外部资料。
- 三个会话均在提交 `3276fa3` 上运行；生成结束后锁定原始输出，再与断言核对。
- 第二轮复用了相同测试输入，以验证四项规则回归；运行会话和上下文均为全新。

## 任务记录

| 测试 | 独立 Codex 会话 ID | 原始输入 | 锁定的原始输出 |
|---|---|---|---|
| D | `01a074e0-4cb8-7513-8406-caaa62ca7660` | [服饰商品属性提取与上架 Copilot](../inputs/01-apparel-listing-copilot.md) | [第二轮输出](../outputs/blind-round-2/01-apparel-listing-copilot.md) |
| E | `01a07511-0328-7373-9e23-bc5a78668f13` | [服饰趋势预测与款式设计 Copilot](../inputs/02-fashion-trend-design.md) | [第二轮输出](../outputs/blind-round-2/02-fashion-trend-design.md) |
| F | `01a07510-fbbb-74c0-810c-adce75063aaf` | [电商跨渠道全自动定价 Agent](../inputs/03-cross-channel-pricing-agent.md) | [第二轮输出](../outputs/blind-round-2/03-cross-channel-pricing-agent.md) |

## 结果摘要

| 测试 | 预期结论 | 实际结论 | 证据状态 | 五道门关键状态 | 当前严格断言 |
|---|---|---|---|---|---|
| D | `GO` | `GO` | `SUFFICIENT` | 五门均 `PASS`；规模化 ROI 单列为 `NOT-ASSESSABLE` | 通过 |
| E | `VALIDATE-FIRST` | `VALIDATE-FIRST` | `PARTIAL` | 无门因缺材料误判 `FAIL` | 通过 |
| F | `NO-GO` | `NO-GO` | `SUFFICIENT` | 第一门 `PASS`；第二、三、五门 `FAIL` | 通过 |

- 三类核心决策：`3/3` 通过。
- 四项定向修复断言：`4/4` 通过。
- 按当前规则校准后的严格用例：`3/3` 通过。
- 未改动任何盲测原始输出。

## 四项修复核验

### 1. 证据来源边界：通过

- D 明确说明底层日志、访谈、POC 记录和审批原件均未随附，将 E01–E12 全部保留为 `documented_claim`，并声明项目级 `verified_fact` 为空。
- F 将制度、回放、财务和治理信息保留为 `documented_claim`；没有因版本号、精确数字或“多方确认”升级为 `verified_fact`。

### 2. NO-GO 证据充分度：通过

F 的执行结论为 `NO-GO / 不应该做`，总体证据状态为 `SUFFICIENT`。ROI、模型比较等非决定性缺口单列为未知，没有削弱权限冲突、P0 超阈值、粗粒度写权限和无可靠回滚对当前范围的否决。

### 3. 第一门独立判断：通过

F 将第一门判为 `PASS`，同时把北极星指标和收益口径记为度量准备度缺口。第二、三、五门的失败没有反向推导成“需求不存在”。

### 4. 最小验证切片：通过

E 没有编造企业未给出的具体品类名称，而是要求启动前书面冻结：一个具体女装品类、下一秋冬企划季的一次完整趋势研究周期、一个 AI 候选节点、有限真实任务样本和前置验证子预算；同时明确排除完整平台、自动选款打样、外部发布和跨品类扩展。

## 测试基准校准记录

核对过程中发现原 `03-no-go-assertions.md` 同时要求“输入未提供制度和回放原件，不得升级为 `verified_fact`”以及“高置信度、已验证负面证据”。这与本轮修复后的证据边界不一致。

因此在不修改原始输出的前提下，校准未来测试口径：当前输入只含汇总材料时，预期为 `SUFFICIENT + 中置信度`，决定性主张仍为 `documented_claim`；只有实际提供并检查制度原文和逐条回放记录时才允许高置信度或 `verified_fact`。该变更属于测试预言修正，不是对盲测结果的重写。

## 结论与边界

第二轮证明四项目标修复均已生效，三类决策和当前严格断言全部通过。该结果支持发布候选版，但仍只是三个模拟场景、单次运行的前向测试，不代表跨模型、多次重复或真实企业项目上的统计稳定性。
